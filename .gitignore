# Python
__pycache__/
*.py[cod]
*.egg-info/

# Ambientes virtuais
.venv/
venv/
env/

# Variáveis de ambiente / segredos
.env

# Django
*.sqlite3
db.sqlite3
db.sqlite3-journal
local_settings.py
staticfiles/
media/
*.log

# Leilão (segunda aplicação): banco, uploads e estáticos coletados próprios.
# O SQLite em modo WAL cria os arquivos -wal/-shm ao lado do banco.
*.sqlite3-wal
*.sqlite3-shm
media_leilao/
staticfiles_leilao/
test_leilao.sqlite3

# Dados de migração do sistema antigo (dados pessoais de menores — NUNCA versionar)
exportacao_migracao_*.zip
migracao/

# Extratos bancários (dados financeiros/pessoais — NUNCA versionar)
EXTRATOS/
extratos/
*.ofx

# Documentos/arquivos soltos na raiz (ex.: PDFs de referência escaneados) — não versionar
/*.pdf

# Levantamento de migração da diretoria (contém nomes reais/logins) — não versionar
docs/MIGRACAO_DIRETORIA.md
# Config local da migração da diretoria (skip/mesclagem com logins reais) — não versionar
migracao_mesclagem.json

# Editores / SO
.vscode/
.idea/
.DS_Store
Thumbs.db
